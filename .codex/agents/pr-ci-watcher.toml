name = "pr-ci-watcher"
description = "Load the canonical pr-ci-watcher agent from .claude/agents/pr-ci-watcher.md."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
developer_instructions = """
Before acting, locate the repository root with `git rev-parse --show-toplevel`, then read `.claude/agents/pr-ci-watcher.md` from that root completely and follow its body as your role instructions. Ignore its Claude-specific YAML frontmatter.
"""
